# packages/

Future home of shared packages across `apps/web` and `apps/app` (e.g., brand tokens, shared config).

**Empty by design.** Do not introduce workspace/monorepo tooling (pnpm workspaces, npm workspaces, turborepo) until there is real shared code that justifies it. The two apps use different stacks and are independent today.
