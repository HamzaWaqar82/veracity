# Requirements docs are local-only by decision — never commit
docs/

# Design-tool working state (DESIGN.md is the committed artifact)
.impeccable/

# JS/TS
node_modules/
.next/
out/
*.tsbuildinfo

# Lead capture (server-side form storage)
apps/web/frontend/data/

# Lighthouse raw per-audit reports (committed artifact = the summaries only)
apps/web/lighthouse/raw/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ruff_cache/

# Env / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
